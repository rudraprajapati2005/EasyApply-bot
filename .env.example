# Database (change password/host as needed)
DATABASE_URL=postgresql+asyncpg://postgres:rudra@localhost/jobbot

# Redis (for later)
REDIS_URL=redis://localhost:6379/0

# App
APP_HOST=0.0.0.0
APP_PORT=8000
ENV=development
